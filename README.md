# CV

LaTeX source for my CV, built with XeLaTeX.

## Build

```sh
make        # builds cv.pdf
make open   # builds and opens cv.pdf
make clean  # removes build artifacts
```

Requires a XeLaTeX installation (e.g. via [MacTeX](https://www.tug.org/mactex/) or TeX Live).
